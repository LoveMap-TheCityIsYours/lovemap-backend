apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: smackmap-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/smackmap-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/smackmap-network: "true"
